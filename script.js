document.querySelector("#colete-infantil-personalizado").addEventListener("click",function(){const phoneNumber="551126429801";const message="Olá, vim através do site e gostaria de solicitar um orçamento de um colete infantil personalizado";const encodedMessage=encodeURIComponent(message);const whatsappUrl=`https://wa.me/${phoneNumber}?text=${encodedMessage}`;window.open(whatsappUrl,"_blank")});document.querySelector(".whatsapp-req-orcamento").addEventListener("click",function(){const phoneNumber="551126429801";const message="Olá, vim através do site e gostaria de solicitar um orçamento de um produto";const encodedMessage=encodeURIComponent(message);const whatsappUrl=`https://wa.me/${phoneNumber}?text=${encodedMessage}`;window.open(whatsappUrl,"_blank")});window.addEventListener('load',function(){setTimeout(()=>{window.scrollTo(0,0)},100)});document.addEventListener('DOMContentLoaded',function(){const searchInput=document.getElementById('search-input');const clearSearchBtn=document.getElementById('clear-search');const searchResultsCount=document.getElementById('search-results-count');const productItems=document.querySelectorAll('.product-item');const filterBtns=document.querySelectorAll('.filter-btn');let currentFilter='todos';let currentSearchTerm='';function productMatchesCategory(product,category){if(category==='todos')return!0;const categories=product.dataset.categories;if(!categories)return!1;const categoryList=categories.split(',').map(cat=>cat.trim());return categoryList.includes(category)}
function performSearchAndFilter(searchTerm,filterCategory){const searchTermLower=searchTerm.toLowerCase().trim();let visibleCount=0;productItems.forEach(item=>{const title=item.querySelector('h3').textContent.toLowerCase();const description=item.querySelector('p').textContent.toLowerCase();const matchesSearch=searchTermLower===''||title.includes(searchTermLower)||description.includes(searchTermLower);const matchesFilter=productMatchesCategory(item,filterCategory);if(matchesSearch&&matchesFilter){item.style.display='flex';visibleCount++;item.style.opacity='1';item.style.transform='scale(1)'}else{item.style.display='none'}});if(searchTermLower.length>0||filterCategory!=='todos'){searchResultsCount.textContent=`${visibleCount} produto(s) encontrado(s)`;searchResultsCount.style.display='inline-block'}else{searchResultsCount.style.display='none'}
clearSearchBtn.style.display=(searchTermLower.length>0||filterCategory!=='todos')?'block':'none'}
searchInput.addEventListener('input',function(e){const searchTerm=e.target.value;currentSearchTerm=searchTerm;performSearchAndFilter(searchTerm,currentFilter)});clearSearchBtn.addEventListener('click',function(){searchInput.value='';currentSearchTerm='';performSearchAndFilter('',currentFilter);searchInput.focus()});searchInput.addEventListener('keypress',function(e){if(e.key==='Enter'){e.preventDefault();const firstVisibleProduct=Array.from(productItems).find(item=>item.style.display!=='none');if(firstVisibleProduct){firstVisibleProduct.scrollIntoView({behavior:'smooth',block:'center'})}}});const searchBox=document.querySelector('.search-box');searchBox.addEventListener('click',function(){searchInput.focus()});searchInput.addEventListener('focus',function(){searchBox.classList.add('search-focused');if('ontouchstart' in window||navigator.maxTouchPoints>0){searchInput.setAttribute('inputmode','text');searchInput.setAttribute('autocomplete','on');searchInput.setAttribute('autocorrect','on');searchInput.setAttribute('autocapitalize','sentences');setTimeout(()=>{searchInput.focus();searchInput.click()},100)}});searchInput.addEventListener('blur',function(){setTimeout(()=>{if(!searchInput.matches(':focus')&&!searchBox.matches(':hover')){searchBox.classList.remove('search-focused')}},100)});filterBtns.forEach(btn=>{btn.addEventListener('click',function(){filterBtns.forEach(b=>b.classList.remove('active'));this.classList.add('active');currentFilter=this.dataset.category;performSearchAndFilter(currentSearchTerm,currentFilter)})});let searchTimeout;searchInput.addEventListener('input',function(e){clearTimeout(searchTimeout);searchTimeout=setTimeout(()=>{currentSearchTerm=e.target.value;performSearchAndFilter(currentSearchTerm,currentFilter)},300)});if('ontouchstart' in window||navigator.maxTouchPoints>0){searchInput.setAttribute('inputmode','text');searchInput.setAttribute('autocomplete','on');searchInput.setAttribute('autocorrect','on');searchInput.setAttribute('autocapitalize','sentences');searchInput.style.fontSize='16px'}})